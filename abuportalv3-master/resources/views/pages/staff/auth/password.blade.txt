<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <title>Ahmadu Bello University::Portal</title>
    @include("commons.staff.head")
    @yield("css")
</head>
<body class="boxed-layout pt-40 pb-40 pt-sm-0" style="background: #999 url('{{ asset('pattern/02.png') }}')">

<div id="wrapper" class="clearfix">
    <!-- preloader -->
    <div id="preloader">
        <div id="spinner">
            <img alt="" src="{{ asset("images/preloaders/5.gif") }}">
        </div>
        <div id="disable-preloader" class="btn btn-default btn-sm">Disable Preloader</div>
    </div>

    <!-- Header -->
    <!-- Start main-content -->
    <div class="row">
        <div class="col-md-4 col-md-offset-4 pb-50 pt-40">
            <div class="bg-lightest p-25 boxshadow" style="border-left:1px solid #43b14b; ">
                <div class="text-center mb-10">
                    <a class="">

                        <img alt="ABU Staff Portal" src="{{ asset("images/logo64.png") }}">

                    </a>
                </div>
                <h4 class="text-theme-colored text-uppercase m-0 text-center">STAFF PORTAL</h4>
                <div class="line-bottom-centered mb-30"></div>
                @if($errors->has("message"))
                    <p class="alert alert-warning">
                        {{$errors->first("message")}}
                    </p>
                @endif
                <form id="login" name="login" class="mt-30"
                      action="{{ route("staff.reset.password",[Request::route('locale')]) }}" method="post" autocomplete="off">
                    <div class="row">
                        @csrf
                        @php
                            $usr = Str::random(rand(6,12));
                        @endphp
                        <input type="hidden" name="lang" value="{{ Request::route('locale') }}">
                        <input autocomplete="off" name="hidden" type="text" style="display:none;">
                        <input type="hidden" name="uilwaknscgfdjp" value="{{ encrypt($usr."\$altu\$r") }}"
                               autocomplete="off">

                        <div class="col-sm-12">
                            <div class="form-group mb-10">
                                <label>Username</label>
                                <input name="{{$usr}}" class="form-control btn-flat" type="text" id="username" readonly
                                       required placeholder="Enter username" aria-required="true">
                            </div>
                        </div>
                    </div>

                    <div class="form-group mb-0 mt-20">
                        <input name="form_botcheck" class="form-control" type="hidden" value="">
                        <button type="submit" class="btn btn-success btn-flat btn-lg"
                                data-loading-text="Please wait...">Reset Password
                        </button>
                    </div>
                </form>

            </div>
            @include("commons.staff.footer")
        </div>
    </div>
</div>

@include("commons.staff.scripts")
<script>
    $(function () {
        $("#username").on('click', function () {
            $(this).attr("readonly", false);
        });
    });
</script>
</body>
</html>
