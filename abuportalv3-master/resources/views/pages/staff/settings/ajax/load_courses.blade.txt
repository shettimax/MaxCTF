<div style="position: relative;" class="table-responsive">
    <table id='courses' class="table table-striped table-bordered table-hover table-condensed nowrap">
        <thead class="thead-light">
            <tr  class="text-dark text-uppercase">
                <th>S/N</th>
                <th>Code</th>
                <th>Title</th>
                <th>Unit</th>
                <th>Semester</th>
                <th>Level</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody style="text-align: start">
            @foreach($records as $record)
                <tr>
                    <td>{{ $loop->iteration }}</td>
                    <td>{{ $record->coursecode }}</td>
                    <td>{{ $record->coursetitle }}</td>
                    <td style="text-align: center">{{ $record->courseunit }}</td>
                    <td>{{ $record->coursesemester == 1 ? "First" : "Second" }}</td>
                    <td>{{ $record->level  }}</td>
                    <td>
                        <a class='btn btn-sm btn-primary edit' data-toggle='modal' data-target='#add-edit' data-id='{{  $record->id }}' 
                            data-code="{{ $record->coursecode }}" data-title="{{ $record->coursetitle }}" data-unit="{{ $record->courseunit }}"
                            data-semester="{{ $record->coursesemester }}" data-level="{{ $record->level_id }}" data-department="{{ $record->department_id }}"
                            data-faculty="{{ $record->faculty_id }}"
                            href='javastript:;'>
                            <span class='icon icon_pencil-edit'></span> Edit
                        </a>
                    </td>
                </tr>
            @endforeach
        </tbody>
    </table>
</div>
    