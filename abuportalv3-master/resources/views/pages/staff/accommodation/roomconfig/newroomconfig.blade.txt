@extends('layout.staff.master')
@section('css')
<style rel="text/css">
</style>
@endsection
@section('content')
    <section class="">

        <div class="container">
                <div class='col-12'>
                    <h2 class="text-theme-colored2 font-20">CREATE NEW SESSIONAL ROOM CONFIGURATION</h2>
                    <hr />
                    <br />
                    <br />
                    @if($accommodation->status != 'terminated' && $maxsession < $accommodation->session)
                        <form action="{{route('accommodation.admin.roomconfig.storeall')}}" method="POST">
                            <div class="form-controls">
                                <label for="configsession" class=""><strong>New Configuration Session:</strong>
                                    <input type="text" readonly class="form-control plaintext" name='configsession' id="configsession" value="{{$accommodation->session}}">
                                </label>
                            </div>
                            <div class="form-controls">
                                <label for="prototypesession"><strong>Inherit Room Configurations From:</strong>
                                    <select class="form-control" id="prototypesession" name='prototypesession'>
                                        @foreach($previousacc as $pa)
                                            <option value='{{$pa->session}}'>{{$pa->session}}</option>
                                        @endforeach
                                    </select>
                                </label>
                            </div>
                            <input type="hidden" name='session' value="{{$accommodation->session}}" />
                            <input type='hidden' name='_token' value='{{csrf_token()}}' />
                            <button type="submit" class="btn btn-primary mb-2">Proceed</button>
                        </form>
                    @else
                        <div class='alert alert-info' role='alert'>
                            There are active room configurations.  <a href="{{route('accommodation.admin.roomconfig.roomconfigaction')}}" class="btn btn-sm btn-primary">Modify Existing Room Configurations</a> instead.
                        </div>
                    @endif
                </div>
        </div>
    </section>
@endsection
@section('js')
<script type='text/javascript'>
</script>
@endsection
