<div class="text-right"><a href='#' class="btn btn-sm btn-default createroomaction" data-id='{{$block->id}}'>Add New Room in {{$block->name}} Block</a></div>
<table class="table table-bordered table-hover table-sm">
    <thead class='thead-dark'>
        <tr>
            <th>S/N</th>
            <th>Room No.</th>
            <th>Bespace</th>
            <th>Block</th>
            <th>Hostel</th>
            <!--<th>Status</th>-->
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach($rooms as $room)
        <tr>
            <td>{{$loop->iteration}}</td>
            <td><a href='#' class='viewroom' data-id='{{$room->id}}'>{{strtoupper($room->room_no)}}</a></td>
            <td>{{ $room->bed_space }}</td>
            <td>{{strtoupper($room->block->name)}}</td>
            <td>{{strtoupper($room->block->hostel->name)}}</td>
            <!--<td>(room->status==1)?('Active'):('Inactive')</td>-->
            <td>
                <a href="#" class="viewroom btn btn-default btn-sm" data-id='{{$room->id}}'>View</a>
                <a href="#" class="modifyroomaction btn btn-default btn-sm" data-id='{{$room->id}}'>Modify</a>
            </td>
        </tr>
        @endforeach
    </tbody>
</table>
