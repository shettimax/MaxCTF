<!-- resources/views/config-card.blade.php -->

<div class="config-card" id="config-{{ $config->id }}" data-id="{{ $config->id }}" data-parent="{{ $config->parent_id }}">
    <div class="card-header">
        <span>{{ $config->interpretConfigs() }}</span>
        <button class="btn btn-danger btn-sm delete-card" style="height:24px;padding:4px 12px !important;float:right;" type="button" data-id="{{$config->id}}">Delete</button>
    </div>
    <!-- Drop area for child cards -->

    <div class="child-zone" id="child-zone-{{ $config->id }}">
        @if(!empty($config->children))
                <!-- Render child configurations recursively -->
                @foreach($config->children as $child)
                    @include('pages.staff.accommodation.config.config-card', ['config' => $child])
                @endforeach
        @endif
    </div>
</div>
