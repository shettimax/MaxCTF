$apps->firstname = $appt->firstname;
$apps->surname = $appt->surname;
$apps->othernames = $appt->othernames;
$apps->dob = $appt->dob;
$apps->marital_status = $appt->marital_status;
$apps->gender = $appt->gender;
$apps->lga_of_origin = $appt->lga_of_origin;
$apps->state_of_origin = $appt->state_of_origin;
$apps->nationality = $appt->nationality;
$apps->no_of_depend_child = $appt->no_of_depend_child;
$apps->id_card_type = $appt->id_card_type;
$apps->id_card_no = $appt->id_card_no;
$apps->id_card_issue_date = $appt->id_card_issue_date;
$apps->id_card_expiry_date = $appt->id_card_expiry_date;
$apps->alternative_occupation = $appt->alternative_occupation;
$apps->farm_geo_coordinate = $appt->farm_geo_coordinate;
$apps->bvn = $appt->bvn;
$apps->phone = $appt->contact->phone;
$apps->street = $appt->contact->street;
$apps->city = $appt->contact->city;
$apps->lga_id = $appt->contact->lga;
$apps->state_id = $appt->contact->state;
$apps->save();

$app = Applicant::orderBy('id', 'DESC')->first();

$contact->apps_id = $app->id;
$contact->street = $appt->contact->street;
$contact->city = $appt->contact->city;
$contact->lga_id = $appt->contact->lga;
$contact->state_id = $appt->contact->state;

$nok->apps_id = $app->id;
$nok->name = $appt->nok->name;
$nok->phone = $appt->nok->phone;
$nok->address = $appt->nok->address;

$guarantor->apps_id = $app->id;
$guarantor->name = $appt->guarantor->name;
$guarantor->address = $appt->guarantor->address;
$guarantor->lga_id = $appt->guarantor->lga;
$guarantor->state_id = $appt->guarantor->state;
$guarantor->email = $appt->guarantor->email;
$guarantor->phone = $appt->guarantor->phone;
