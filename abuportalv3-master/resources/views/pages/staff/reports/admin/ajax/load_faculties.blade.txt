<div style="position: relative;" class="table-responsive">
    <table id='faculties' class="table table-striped table-bordered table-hover table-condensed nowrap">
        <thead class="thead-light">
            <tr  class="text-dark text-uppercase">
                <th>S/N</th>
                <th>Name</th>
                <th>Nature</th>
                <th>Dean/Director</th>
                <th>Complex</th>
                <th>Campus</th>
            </tr>
        </thead>
        <tbody>
            @foreach($faculties as $faculty)
                <tr>
                    <td>{{ $loop->iteration }}</td>
                    <td>{{ $faculty->name }}</td>
                    <td>
                        @if($faculty->is_other_institute==2)
                            {{ 'Degree Awarding' }}
                        @elseif($faculty->is_other_institute==1)
                            {{ 'Non Degree Awarding' }}
                        @else($faculty->is_other_institute==2)
                            {{ 'Others' }}
                        @endif
                    </td>
                    <td><strong>{{ ucfirst($faculty->head_pno) }}</strong> {{ $faculty->deanName() }}</td>
                    @php $fComplex =\App\Models\Complex::find($faculty->complex_id) ;@endphp
                    <td>{{ $fComplex->name ?? '' }}</td>
                    <td>{{ $faculty->campus }}</td>                                     
                </tr>
            @endforeach
        </tbody>
    </table>
</div>