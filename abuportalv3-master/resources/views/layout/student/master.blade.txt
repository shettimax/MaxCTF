<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <title>Ahmadu Bello University:Portal</title>
    @include("commons.student.head")
    <style>
        .modal-content{
            border-top:5px solid #43B14B;
            border-bottom:5px solid #43B14B;
            border-radius:0;
        }

        .modal-body{
            max-height: 450px;
            overflow-y: scroll;
        }
        .modal-header{
            padding-left:80px;
            z-index: 100;
        }
        .modal-footer{
            z-index: 100;
        }
        .modal-header:after{
            content: "";
            /*display: block;*/
            width: 100px;
            height: 100px;
            position: absolute;
            top: 0;
            left: 0;
            background-image: url("{{ asset('images/logo64.png') }}");
            background-size: 55px 50px;
            background-position: 10px 10px;
            background-repeat: no-repeat;
            /*opacity: 0.5;  */
            z-index: auto;
        }

        .student-status-red{
            padding-bottom:1px;
            background: rgb(255,255,255);
            background: linear-gradient(90deg, rgba(255,255,255,1) 50%, rgba(255,0,0,1) 100%);
            border-top:5px solid #ffffff;
        }
        .student-status-red .red{
            background: #ffffff;
            color:darkred;
        }
        .student-status-green{
            padding-bottom:1px;
            background: rgb(255,255,255);
            background: linear-gradient(90deg, rgba(255,255,255,1) 50%, rgba(67,177,75,1) 100%);
            border-top:5px solid #ffffff;
        }
        .student-status-green .green{
            background: #ffffff;
            color:darkgreen;
        }
        .student-status-orange{
            padding-bottom:1px;
            background: rgb(255,255,255);
            background: linear-gradient(90deg, rgba(255,255,255,1) 50%, rgba(255,165,0,1) 100%);
            border-top:5px solid #ffffff;
        }
        .student-status-orange .orange{
            background: #ffffff;
            color: orange;
        }
    </style>
    @yield("css")
</head>
<body class="boxed-layout pt-40 pb-40 pt-sm-0" data-bg-img="images/pattern/p5.png">

<div id="wrapper" class="clearfix">
    <!-- preloader -->
    <div id="preloader">
        <div id="spinner">
            <img alt="" src="{{ asset("images/preloaders/5.gif") }}">
        </div>
        <div id="disable-preloader" class="btn btn-default btn-sm">Disable Preloader</div>
    </div>

    <!-- Header -->
    @include("commons.staff.header")
    <!-- Start main-content -->
    <div class="main-content">
        <!-- Section: Pricing -->
        <section class="inner-header divider layer-overlay" style="box-shadow: inset;width: 99%;margin:0 auto;"  id="overlaysection">
            <div class="container" style="padding: 2px 0;">
            </div>
        </section>
        <section id="menusection">
            <div class="container" style="padding:0;" >
                <div class="menuzord" style="padding: 0;">
                    @include("commons.student.menu")
                </div>
            </div>
        </section>

        @auth("student")

            <section class="inner-header divider layer-overlay" style="box-shadow: inset;width: 99%;margin:0 auto;height: 4px;" id="overlaysection2">
                <div class="container"  style="height: 5px;">

                </div>
            </section>
            @php
                $student = Auth::user();
                $status = $student->status($student->currentSession()->session);

            @endphp
        <section class="pl-30 pr-30 student-status-{{$status->color}}" style="">
            <div class="section-content">
                <div class="row">
                    <div class="col-sx-12 col-sm-12 col-md-12 text-right">
                        <!-- graduated -->

                        <span class="badge {{ $status->color }}">{{ $status->status }}</span>
                        <!-- active -->
                        {{--<span class="badge green">Active</span>--}}
                        <!-- overstayed -->
                        {{--<span class="badge red">Overstayed</span>--}}
                    </div>
                </div>
            </div>
        </section>
        @endauth

        @yield("content")

    </div>
    <!-- Footer -->
    @include("commons.staff.footer")
</div>
<!-- end wrapper -->


@include("commons.staff.scripts")
@yield("js")
</body>
</html>
